<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="PTSortableNinePhotoLayout">
        <!-- 是否显示加号，默认值为 true -->
        <attr name="snpl_plusEnable" format="boolean"/>
        <!-- 是否可拖拽排序，默认值为 true -->
        <attr name="snpl_sortable" format="boolean"/>
        <!-- 删除按钮图片，默认值为 R.mipmap.pp_ic_delete -->
        <attr name="snpl_deleteDrawable" format="reference"/>
        <!-- 可选择图片的总张数,默认值为 9 -->
        <attr name="snpl_maxItemCount" format="integer"/>
        <!-- 列数,默认值为 3 -->
        <attr name="snpl_itemSpanCount" format="integer"/>
        <!-- 删除按钮是否重叠四分之一，默认值为 false -->
        <attr name="snpl_deleteDrawableOverlapQuarter" format="boolean"/>
        <!-- 添加按钮图片，默认值为 R.mipmap.ic_plus -->
        <attr name="snpl_plusDrawable" format="reference"/>
        <!-- Item 条目圆角尺寸，默认值为 0dp -->
        <attr name="snpl_itemCornerRadius" format="dimension|reference"/>
        <!-- Item 间的水平和垂直间距，默认值为 4dp -->
        <attr name="snpl_itemWhiteSpacing" format="dimension|reference"/>
        <!-- 出去九宫格部分的空白区域的尺寸，默认值为 100dp -->
        <attr name="snpl_otherWhiteSpacing" format="dimension|reference"/>
        <!-- 占位图资源，默认值为 R.mipmap.ic_holder_light -->
        <attr name="snpl_placeholderDrawable" format="reference"/>
        <!-- 是否可编辑，默认值为 true -->
        <attr name="snpl_editable" format="boolean"/>
        <!-- item 的尺寸，大于 0dp 时优先级高于 snpl_otherWhiteSpacing，默认值为 0dp -->
        <attr name="snpl_itemWidth" format="dimension|reference"/>
    </declare-styleable>

    <declare-styleable name="PTNinePhotoLayout">
        <!-- Item 条目圆角尺寸，默认值为 0dp -->
        <attr name="npl_itemCornerRadius" format="dimension|reference"/>
        <!-- 当只有一张图片时，是否显示成大图，默认值为 true -->
        <attr name="npl_showAsLargeWhenOnlyOne" format="boolean"/>
        <!-- Item 间的水平和垂直间距，默认值为 4dp -->
        <attr name="npl_itemWhiteSpacing" format="dimension|reference"/>
        <!-- 出去九宫格部分的空白区域的尺寸，默认值为 100dp -->
        <attr name="npl_otherWhiteSpacing" format="dimension|reference"/>
        <!-- 占位图资源，默认值为 R.mipmap.ic_holder_light -->
        <attr name="npl_placeholderDrawable" format="reference"/>
        <!-- item 的尺寸，优先级高于 npl_otherWhiteSpacing，默认值为 0dp -->
        <attr name="npl_itemWidth" format="dimension|reference"/>
        <!-- 列数,默认值为 3，当该值大于 3 并且数据源里只有四张图片时不会显示成 2 列 -->
        <attr name="npl_itemSpanCount" format="integer"/>
    </declare-styleable>

    <declare-styleable name="PTImageView">
        <!-- 默认图片资源，默认值为 null -->
        <attr name="android:src"/>
        <!-- 是否是圆形，默认值为 false -->
        <attr name="iv_circle" format="boolean"/>
        <!-- 圆角矩形的半径，默认值为 0dp -->
        <attr name="iv_cornerRadius" format="reference|dimension"/>
        <!-- 是否是矩形，默认值为 false -->
        <attr name="iv_square" format="boolean"/>
        <!-- 描边的宽度，默认值为 0dp -->
        <attr name="iv_borderWidth" format="reference|dimension"/>
        <!-- 描边的颜色，默认值为 Color.WHITE -->
        <attr name="iv_borderColor" format="reference|color"/>
    </declare-styleable>
</resources>